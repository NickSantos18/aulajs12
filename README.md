# 📘 Painel de Tabuadas

Este projeto é um **gerador de tabuadas de 1 a 10**, desenvolvido com **HTML, CSS e JavaScript**.
Ao clicar no botão **“Gerar Tabuadas”**, o sistema cria automaticamente cards organizados com todas as multiplicações de cada número.

---

## 🚀 Tecnologias Utilizadas

* **HTML5** → Estrutura da página
* **CSS3** → Estilização e efeitos visuais
* **JavaScript** → Lógica para gerar as tabuadas dinamicamente

---

## ⚙️ Como Funciona

A função `gerarTabuada()`:

* Usa um `while` externo para percorrer os números de **1 a 10**
* Usa outro `while` interno para multiplicar cada número também de **1 a 10**
* Cria dinamicamente o conteúdo em HTML
* Insere tudo dentro da `<div id="painel">` usando `innerHTML`

Cada tabuada é exibida em um **card estilizado**, organizado com **Flexbox** para ficar responsivo.

---

## 🎨 Estilização

O projeto possui:

* Fundo azul
* Botão com efeito hover e animação
* Cards brancos com sombra e bordas arredondadas
* Efeito de leve elevação ao passar o mouse

---

## 📂 Estrutura de Arquivos

```
📁 projeto-tabuada
 ├── index.html
 ├── style2.css
 └── script2.js
```

---

## ▶️ Como Executar

1. Baixe os arquivos
2. Coloque todos na mesma pasta
3. Abra o `index.html` no navegador
4. Clique em **Gerar Tabuadas**

---

## 📚 O que eu pratiquei nesse projeto

* Estruturas de repetição (`while`)
* Manipulação do DOM
* Template Literals (`` `${}` ``)
* Organização de layout com Flexbox
* Estilização com efeitos e transições






